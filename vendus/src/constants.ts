/**
 * API base URL — also the human-readable docs URL (it renders the Redoc
 * documentation viewer in a browser). Single home for the version pin.
 */
const VENDUS_BASE_URL = 'https://www.vendus.pt/ws/v1.2/';

export { VENDUS_BASE_URL };
